on:
  workflow_dispatch:
    inputs:
      environment: 
        description: "choose env"
        required: true
        default: "staging"
        type: choice
        options: 
          - staging
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps: 
      - name: Deploy
        run: echo "Deploy Application to ${{ inputs.environment }}"
